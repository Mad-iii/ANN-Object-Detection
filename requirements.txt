# ──────────────────────────────────────────────────────────
#  requirements.txt  —  Object Detection & Tracking Project
# ──────────────────────────────────────────────────────────
#
#  Install with:
#      pip install -r requirements.txt
#
#  Python 3.9 – 3.11 recommended
# ──────────────────────────────────────────────────────────

# Core vision & video
opencv-python>=4.8.0
numpy>=1.24.0

# YOLOv8 detection
ultralytics>=8.0.0

# DeepSORT tracker
deep-sort-realtime>=1.3.2

# PyTorch (CPU version — safe default)
# If you have an NVIDIA GPU, replace this line with the GPU build:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
torch>=2.0.0
torchvision>=0.15.0
